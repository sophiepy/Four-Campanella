<table class="table table-hover table-inverse">
    <h2>Items search for <%= params[:word] %> </h2>
     <thead>
      <tr>
        <th></th>
        <th>Name</th>
        <th>Intoroduction</th>
        <th>Price</th>
        <th colspan="3"></th>
      </tr>
     </thead>
    <tbody>
      <% @items.each do |item| %>
        <tr>
          <td><%#= item.inspect %><%= image_tag item.get_item_image("300x200") %><%#= image_tag item.user.get_item_image, size:'50x50' %></td>
          <td><%= item.name %></td>
          <td><%= item.introduction %></td>
          <td><%= item.price %>円</td>
          <td><%= link_to 'Show',item_path(item.id) %></td>
        </tr>
     <% end %>
    </tbody>
  </table>